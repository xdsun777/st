# 个人自用刷题App 完整业务规格文档
>
> 用途：交付开发、设计；个人本地使用，不上应用商店，无二期迭代，开发完成即为版本终点
> 核心前提：单用户本地工具，无网络服务，无广告、付费、社交

## 1. 核心业务定位

- **目标用户**：个人自用，仅自己使用。
- **用户痛点**
  1. 通用刷题App题库固化，无法导入自己整理的习题。
  2. 第三方App充斥会员、广告、社交等无关功能造成干扰。
  3. 希望学习数据完全保存在本机，不依赖外网；需要错题复盘、正确率统计定位薄弱点。
- **产品定位**：一款个人自用刷题工具，支持自定义导入题库，完成刷题、错题整理、复习复盘，服务个人学习，无广告无社交。

## 2. 业务边界

### 需要做

- CSV格式导入自制题库
- 本地刷题：单选、多选、判断、简答、填空
- 自动错题本、手动题目收藏
- 顺序/随机刷题模式、错题复习模式
- 做题记录、正确率基础统计
- 题目按题库集、标签管理
- 全部数据本地优先存储
- 数据备份导出、备份恢复

### 坚决不做

- 多用户、注册登录账号体系
- 付费、会员、广告、商城
- 社区、分享、排行、打卡社交
- 爬虫抓取网络公开题库
- 直播、网课、资料下载
- Excel直接导入、题目搜索、自定义组卷、单题笔记、限时刷题、薄弱点分析、增量备份

## 3. 核心业务场景 & 用户旅程

1. **导入题库**：导入CSV题库文件；解析题目、选项、答案、解析、标签；格式错误给出提示，损坏数据不导入。
2. **题库管理**：查看题库集；新建/重命名/删除题库；给题目打标签；手动新增、编辑、删除单道题目。
3. **发起刷题**：选择题库或标签筛选，支持顺序刷题、随机刷题；中途退出自动保存进度。
4. **答题过程**
    - 单选/多选/判断：提交即展示答案与解析；多选必须全部匹配才算答对，错选、少选、多选均判定错误，答错自动加入错题本。
    - 填空：用户输入作答文本，机器自动判分；支持多标准答案；允许手动覆写对错状态；机器判错自动进入错题本。
    - 简答：用户输入作答，**手动标记对错**，无机器判分；标记答错后进入错题本。
    - 支持上一题、下一题；可中途退出保存刷题进度。
5. **错题&收藏**：答错自动进错题本；手动收藏/取消收藏；错题本移除错题仅清除标记，原题保留。
6. **复习**：刷错题集、刷收藏题目，支持标签筛选。
7. **数据查看**：查看刷题统计数据。
8. **备份恢复**：导出全量数据包备份；从备份文件恢复本地数据。

## 4. 题库与题目业务规则

### 4.1 题目类型枚举

|类型标识|题型|判分规则|
|---|---|---|
|single|单选题|机器判分，1个正确选项，答错自动入错题|
|multi|多选题|机器判分，必须全部匹配才算正确，答错自动入错题|
|judge|判断题|机器判分，true/false，答错自动入错题|
|essay|简答题|无机器判分，手动标记对错|
|fill|填空题|机器判分，支持多标准答案；允许用户手动覆写对错状态|

#### 填空题判分细节

1. 多标准答案使用英文分号 `;` 分隔，如 `TCP;传输控制协议`，命中任意一个即为正确。
2. 匹配规则：去除首尾空格，**大小写敏感**，中间空格原样比对。
3. 提交后自动判分；机器判定错误自动加入错题本。
4. 用户可手动覆盖对错结果，手动状态优先级高于机器判分。
5. 答题结果页展示：用户输入内容、全部参考答案、解析。

### 4.2 CSV导入字段

|字段|是否必填|说明|
|---|---|---|
|题目ID|否|内部标识，导入自动生成，用户不用填写|
|题目类型|是|single/multi/judge/essay/fill|
|题干|是|题目文本|
|选项|否|单选、多选使用；简答、填空为空|
|正确答案|是|单选A；多选A,B；判断true/false；简答填写参考；填空多答案用`;`分隔|
|解析|否|参考答案解析文本|
|标签|否|多个标签英文逗号分隔，例：数据库,第三章|
|所属题库集|否|归属题库集合|

#### 导入校验规则

1. 仅支持`.csv`文件。
2. 题干为空、类型非法、答案格式错误 → 跳过该行，输出错误行号提示，不写入题库。
3. 重复题干：保留原有题目，不重复新增。

### 4.3 题库集规则

- 可新建、重命名、删除题库集。
- 一道题目仅归属一个题库集，可附加多个标签。
- 删除题库集：二次确认，删除后集合内所有题目一并删除。

### 4.4 标签规则

- 标签依附题目，不属于题库集；一题可多标签。
- 支持按标签筛选题目、筛选错题。
- 删除标签仅清除题目标记，不删除题目。

### 4.5 错题业务规则

1. 单选/多选/判断答错自动加入错题本；
   - 填空题：机器判错自动加入错题，用户手动修改为答对则移出错题；
   - 简答：手动标记答错加入错题本。
2. 同一题目多次做错，错题本仅存一条，记录做错次数。
3. 错题本“移出错题”：仅移除错题标记，原题、做题记录完整保留。
4. 修改原题题干/答案，**不清除历史错题、做题记录**。

### 4.6 收藏题目规则

- 手动点击收藏 / 取消收藏；收藏独立于错题。
- 删除原始题目，自动取消该题收藏。

### 4.7 备份数据包规则

- 导出：完整数据包包含题目、做题记录、错题标记、收藏、标签。
- 恢复：导入备份包**覆盖本地全部数据**，恢复前弹窗风险提示。
- 备份包为自定义格式，不和普通CSV互通。

## 5. MVP全部实现功能清单

### 题库管理

1. CSV文件导入题库，字段校验，输出错误行提示
2. 手动新增、编辑、删除单道题目
3. 题库集：新建、重命名、删除题库集
4. 题目标签管理：打标签、删除标签、按标签筛选题目

### 刷题核心流程

1. 刷题模式：顺序刷题、随机刷题；中途退出自动保存刷题进度
2. 题型：单选、多选、判断、简答、填空
3. 客观题、填空题提交后展示答案+解析；简答手动标记答对/答错；填空支持机器判分，可手动覆写结果
4. 错题自动归集；错题本刷题，可手动移出错题标记
5. 题目收藏/取消收藏；支持刷收藏题目

### 统计与数据安全

1. 基础统计：总题量、正确数、错误数、整体正确率、分题库统计
2. 全部数据本地存储，不上传服务器
3. 全量备份导出数据包、备份恢复，恢复前风险弹窗提示

## 6. 付费&会员规则

无会员、无付费、无内购；全部功能直接开放，无权限锁。

## 7. 非功能业务规则

1. **登录身份**：无登录注册账号体系，打开App直接进入主界面。
2. **数据存储**：所有数据保存在设备本地，不上传服务器；卸载App本地数据丢失，依赖手动备份。
3. **刷题进度**：中途退出保存当前会话进度；切换/新建刷题会话旧进度作废。
4. **统计逻辑**：统计基于本地做题记录；删除题目同步删除该题做题记录，统计同步更新。
5. **交互容错**：大批量导入展示加载提示，防止界面卡死；删除题库集、恢复备份等高危操作，二次弹窗确认。

## 8. 排除不开发需求清单

- 账号登录、多用户体系
- 付费、会员、广告、内购
- 社区、分享、排行、打卡社交
- 网络题库、爬虫抓取题目
- 网课、直播、资料下载
- Excel直接导入、题干搜索、自定义组卷、单题笔记、限时刷题、薄弱点分析、增量备份

> 文档结束
