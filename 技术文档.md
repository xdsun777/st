# 个人自用刷题App 技术方案文档
>
> 配套业务规格文档，仅实现MVP，无二期迭代；个人本地工具，不上应用商店，无后端服务
> 业务背景：支持CSV导入题库，单选/多选/判断/简答/填空，填空机器判分，错题收藏，本地统计，备份恢复；支持桌面端、Android APK

## 目录

1. 项目概述
2. 整体技术选型
3. 系统架构
4. 数据库设计（SQLite）
5. 模块技术实现细节
6. 文件处理规范（CSV导入、备份包）
7. 核心业务逻辑实现说明
8. 工程依赖清单
9. 构建产物与环境要求
10. 风险与注意事项

---

## 1. 项目概述

### 1.1 项目目标

实现本地刷题工具，全部数据存储设备本地，无网络请求，完成题库管理、刷题、错题复习、统计、备份恢复整套闭环。

### 1.2 业务约束

1. 无账号、无登录、无服务端，所有数据本地存储
2. 仅支持CSV导入题库；自定义格式备份包，不与CSV互通
3. 题型：single单选、multi多选、judge判断、essay简答、fill填空；填空支持机器判分+人工覆写结果
4. 高危操作（删除题库、恢复备份）必须二次确认弹窗
5. 开发完成即为终点，不规划后续迭代功能

### 1.3 运行目标平台

- Windows `.exe`
- Linux `AppImage`
- Android `.apk`（自用，不发布应用商店）

---

## 2. 整体技术选型

|层级|技术|版本建议|用途|
|---|---|---|---|
|应用外壳|Tauri 2.0|最新稳定版|Rust内核，跨平台，访问文件系统、SQLite；输出桌面与Android包|
|前端框架|Vue3 + TypeScript|^3.5|页面组件、交互、刷题UI|
|UI样式|Tailwind CSS|^3|页面样式，弹窗、列表、表单|
|状态管理|Pinia|^2|刷题会话、筛选条件内存状态管理|
|本地数据库|tauri‑plugin‑sql(SQLite)|内置|Rust侧持久化全部业务数据|
|CSV解析|PapaParse|^5|前端解析CSV文件，字段校验，收集错误行|
|文件系统|tauri‑plugin‑fs|内置|文件读取、导出备份包|

> 排除方案
>
> 1. Electron：打包体积大，内存开销高
> 2. 纯Web/PWA：浏览器存储易丢失，文件访问受限
> 3. Flutter/Native：多端复用差，开发成本更高

---

## 3. 系统架构
>
> 分层：前端Web层（Vue3） ↔ Tauri Command ↔ Rust后端层（SQLite、文件IO）

1. **Vue前端层**
    - UI渲染：题库列表、刷题页面、错题本、收藏、统计、导入导出弹窗
    - CSV解析、字段校验；填空判分字符串逻辑；表单输入；弹窗交互
    - Pinia维护内存状态：当前刷题会话、筛选标签、加载状态
    - 通过Tauri Command调用Rust能力，**不直接操作数据库与文件**

2. **Rust后端层(Tauri Commands)**
    - SQLite数据库CRUD：题库集、题目、标签关联、做题记录
    - 文件IO：读取CSV文件、导出自定义备份包、恢复备份包
    - 聚合统计查询，大批量数据处理
    - 所有持久化操作统一在此层，前端只传参数

3. **数据流向**

```
前端UI → Tauri Command → Rust(SQLite / fs) → 本地磁盘
Rust查询结果 → 返回前端渲染
```

> 网络约束：**整个应用不发起任何网络请求，无网络相关代码**。

---

## 4. 数据库设计（SQLite）
>
> 核心四张表，不单独建立错题表、收藏表；标记字段存储在做题记录，贴合业务规则。

```sql
-- 题库集
CREATE TABLE question_bank (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  create_time INTEGER NOT NULL
);

-- 题目主表
CREATE TABLE question (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  bank_id INTEGER NOT NULL REFERENCES question_bank(id) ON DELETE CASCADE,
  q_type TEXT NOT NULL, -- single / multi / judge / essay / fill
  content TEXT NOT NULL, -- 题干
  options TEXT, -- JSON字符串存储选项，简答/填空为null
  answer TEXT NOT NULL, -- 答案；填空多答案英文分号;分隔
  analysis TEXT, -- 解析，可为null
  create_time INTEGER NOT NULL,
  update_time INTEGER NOT NULL
);

-- 标签
CREATE TABLE tag (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL UNIQUE
);

-- 题目-标签多对多关联
CREATE TABLE question_tag (
  question_id INTEGER NOT NULL REFERENCES question(id) ON DELETE CASCADE,
  tag_id INTEGER NOT NULL REFERENCES tag(id) ON DELETE CASCADE,
  PRIMARY KEY(question_id, tag_id)
);

-- 做题记录表：同时承载错题、收藏、作答记录
CREATE TABLE answer_record (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  question_id INTEGER NOT NULL REFERENCES question(id) ON DELETE CASCADE,
  user_answer TEXT, -- 用户作答文本
  machine_result INTEGER, -- 0错误 1正确；简答为null
  manual_result INTEGER, -- 人工覆写结果 null/0/1，优先级高于machine_result
  is_fault INTEGER NOT NULL DEFAULT 0, -- 是否错题 0否1是
  is_collect INTEGER NOT NULL DEFAULT 0, -- 是否收藏 0否1是
  fault_count INTEGER NOT NULL DEFAULT 0, -- 做错累计次数
  finish_time INTEGER NOT NULL
);

-- 刷题会话：保存中途退出的刷题进度
CREATE TABLE practice_session (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  bank_id INTEGER,
  tag_filter TEXT, -- JSON存储筛选标签
  current_index INTEGER NOT NULL DEFAULT 0,
  practice_mode TEXT NOT NULL, -- order / random
  create_time INTEGER NOT NULL
);
```

### 业务说明

1. `question.options` 使用JSON存储选项数组，例如`["A.xxx","B.xxx"]`
2. `manual_result`不为null时，以此作为最终对错状态，覆盖机器判分结果
3. `ON DELETE CASCADE`：删除题库集自动删除题目；删除题目自动删除关联标签、做题记录
4. `practice_session`只保留一份有效会话，新建刷题时覆盖旧会话。

---

## 5. 模块技术实现细节

### 5.1 题库管理模块

1. **CSV导入流程**
    1. 前端唤起文件选择，读取CSV原始文本
    2. PapaParse解析，逐行校验字段：必填项、type枚举值合法性
    3. 收集错误行号+错误信息，不合法行直接跳过，UI展示错误汇总
    4. 将合法数据封装参数，调用Tauri Command传给Rust层批量写入SQLite
2. 单题CRUD：前端表单 → Tauri Command → SQLite增删改查
3. 题库集：新建、重命名、删除；删除前前端弹窗二次确认
4. 标签管理：新增、删除标签；给题目绑定/解绑标签；按标签ID筛选题目（Rust SQL查询）

### 5.2 刷题模块

1. 刷题模式：顺序 / 随机；随机逻辑在Rust层做SQL查询排序，返回题目id列表
2. 会话保存：中途退出，把当前索引、筛选条件存入`practice_session`；再次进入可继续会话或重置会话
3. 题型处理
    - single/multi/judge：前端提交，Rust查询标准答案，前端做判分渲染；答错标记`is_fault=1`，`fault_count+1`
    - fill填空：前端执行判分逻辑
        - 去除用户输入首尾空格；标准答案按`;`分割数组
        - **大小写敏感，中间空格不处理**，命中任意一个标准答案即机器判正确
        - 页面允许手动修改对错，写入`manual_result`字段，优先级最高
    - essay简答：无机器判分，仅用户手动设置`manual_result`
4. 提交答题后，生成一条`answer_record`记录存入数据库。

### 5.3 错题 & 收藏模块

1. 错题本：查询`answer_record.is_fault = 1`，支持标签筛选；错题本移除错题仅更新`is_fault=0`，**不删除做题记录**
2. 收藏：更新`answer_record.is_collect`；可单独刷收藏题目
3. 修改原题题干/答案：**不改动历史answer_record记录**，完全遵循业务规则。

### 5.4 统计模块
>
> 统计全部在Rust层通过SQL聚合计算，返回简单数值给前端渲染

- 全局统计：总刷题量、正确数、错误数、正确率
- 分题库统计：按bank_id分组统计
- 删除题目时，级联删除answer_record，统计数据自动同步更新。

### 5.5 备份 & 恢复模块

1. **备份导出**
    - Rust读取全部业务表：question_bank、question、tag、question_tag、answer_record
    - 序列化为完整JSON结构，写入自定义后缀备份文件（例如`.qpbackup`）
    - 备份包**不兼容普通CSV**
2. **备份恢复**
    1. 用户选择备份文件，前端弹出风险确认弹窗：将覆盖全部本地现有数据
    2. 用户确认后，Rust清空全部业务表，解析备份JSON批量写入数据库
    3. 异常捕获：文件损坏、格式错误，UI提示恢复失败，原有数据保持不变。

### 5.6 UI与容错

1. 大批量导入题目展示加载状态，防止界面卡死
2. 删除题库集、恢复备份，必须二次确认弹窗
3. 所有IO、数据库操作捕获异常，前端友好提示，不崩溃。

---

## 6. 文件处理规范

### 6.1 CSV导入字段规范

|字段|是否必填|说明|
|---|---|---|
|题目ID|否|内部标识，导入自动生成，用户不用填写|
|题目类型|是|single/multi/judge/essay/fill|
|题干|是|题目文本|
|选项|否|单选、多选使用；简答、填空为空|
|正确答案|是|单选A；多选A,B；判断true/false；简答填写参考；填空多答案用`;`分隔|
|解析|否|参考答案解析文本|
|标签|否|多个标签英文逗号分隔，例：数据库,第三章|
|所属题库集|否|归属题库集合|

- 仅接受`.csv`，其他格式直接拒绝导入。

### 6.2 备份包格式

自定义后缀`.qpbackup`，JSON序列化二进制文件，只用于本App备份恢复，不做通用交换格式。

---

## 7. 核心业务逻辑实现说明

### 7.1 填空题机器判分逻辑（前端TS伪代码）

```typescript
/**
 * @param userInput 用户输入
 * @param stdAnswer 原始标准答案，多答案用 ; 分割
 * @returns boolean 是否机器判定正确
 */
function judgeFillAnswer(userInput: string, stdAnswer: string): boolean {
  const input = userInput.trim()
  const stdList = stdAnswer.split(';').map(s => s.trim())
  return stdList.includes(input)
}
```

> 注意：大小写敏感；只去除首尾空格，中间空格原样比对；manual_result优先级高于该函数输出。

### 7.2 多选题判分

标准答案逗号分割，用户选项集合必须完全一致，多选、少选、错选判定错误。

### 7.3 错题规则

1. single/multi/judge机器判错 → `is_fault=1`，`fault_count +=1`
2. fill填空机器判错 → `is_fault=1`；用户手动改为正确，则`is_fault=0`
3. essay简答：只有manual_result=0才置为错题
4. 移出错题：仅设置`is_fault=0`，做题记录保留，不删除行。

---

## 8. 工程依赖清单

### package.json（前端）

```json
{
  "dependencies": {
    "vue": "^3.5.0",
    "pinia": "^2.2.0",
    "papaparse": "^5.4.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.6.0",
    "tailwindcss": "^3.4.0",
    "vite": "^6.0.0"
  }
}
```

### Cargo.toml（Rust侧）

```toml
[dependencies]
tauri = { version = "2.0", features = ["sql", "fs"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
```

---

## 9. 构建产物与环境要求

### 开发环境

1. Node.js >= 20
2. Rust stable 最新版
3. Android编译额外需要 Android SDK、NDK（不需要Android可跳过）

### 输出产物

1. Windows：`.exe` 单文件/安装包
2. Linux：`.AppImage`
3. Android：`.apk`，手动拷贝到手机安装，不上商店

> 开发顺序建议

1. Tauri初始化，SQLite建表
2. CSV导入、题库&题目CRUD、标签
3. 刷题会话、全部题型答题逻辑（填空判分）
4. 错题、收藏业务
5. 统计页面
6. 备份导出、恢复
7. UI优化、弹窗、异常处理

---

## 10. 风险与注意事项

1. **数据安全**：卸载App本地SQLite数据丢失，必须依赖用户手动导出`.qpbackup`备份包。
2. 备份恢复：恢复前务必弹窗提示覆盖风险；解析损坏备份文件不能清空原有库。
3. CSV导入：严格校验，错误行跳过，输出错误行号，不污染数据库。
4. 填空题判分：大小写敏感，业务面向技术刷题，不要自动转大小写。
5. 禁止加入任何网络请求、埋点、上报相关代码，保证纯本地运行。
6. 本项目只实现MVP，二期需求全部不实现。

> 文档结束
